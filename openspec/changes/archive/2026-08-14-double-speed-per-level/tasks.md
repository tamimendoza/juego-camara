## Tasks

1. **Update code constant** – Edit `src/framework/jump_game.py` to change `SPEED_MULTIPLIER` from 1.10 to 2.0.
2. **Update main spec** – Replace all references to the old multiplier in `openspec/specs/pose-jump-game/spec.md` with 2.0.
3. **Validate change** – Run `openspec validate --change double-speed-per-level`.
4. **Run tests** – Execute `pytest -q` to ensure speed progression logic passes.

Mark each task as completed when finished.
